# Playwright disabled for Railway - causes build issues
# Install locally for full Daraz scraping: pip install playwright && playwright install chromium
# playwright==1.48.0
crewai[tools]
groq
python-dotenv
pydantic>=2.0.0
markdown
requests
aiofiles
retrying
tenacity
streamlit>=1.32.0
pandas>=2.0.0
plotly>=5.0.0
